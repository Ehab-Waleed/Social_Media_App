export enum userGender {
    male = "male",
    female = "female"
}

export enum userRole {
    user = "user",
    admin = "admin"
}

export enum userProvider{
    system = "system",
    google = "google"
}